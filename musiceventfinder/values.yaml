# Default values for musiceventfinder
replicaCount: 1

api:
  image:
    repository: ghcr.io/lauradubach/musiceventfinder
    pullPolicy: Always
    tag: "latest"
  
  imagePullSecrets:
    - name: ghcr-secret
  
  service:
    type: ClusterIP
    port: 80
    targetPort: 5000
  
  env:
    dbRootPassword: root
    databaseUri: mysql+mysqlconnector://root:root@msvc-bp-prod-db:3306/msvc-prod
  
  secrets:
    ticketmaster:
      name: ticketmaster-secret
      key: TICKETMASTER_API_KEY

database:
  image:
    repository: mysql
    pullPolicy: IfNotPresent
    tag: "8.4.4"
  
  service:
    type: ClusterIP
    port: 3306
  
  env:
    database: msvc-prod
    rootPassword: root
  
  persistence:
    enabled: true
    storageClass: ""
    accessMode: ReadWriteOnce
    size: 100Mi
  
  probes:
    liveness:
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5
      failureThreshold: 5
    readiness:
      initialDelaySeconds: 30
      periodSeconds: 10

ingress:
  enabled: true
  className: nginx
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix
  tls: []

nameOverride: ""
fullnameOverride: ""

# Secrets configuration
secrets:
  # Set to true to create secrets from values (NOT recommended for production)
  # Set to false if secrets are managed externally
  create: false
  
  # GHCR Docker Registry Secret
  # Only used if secrets.create is true
  ghcr:
    # Base64 encoded .dockerconfigjson
    # Generate with: kubectl create secret docker-registry ghcr-secret --docker-server=ghcr.io --docker-username=USERNAME --docker-password=TOKEN --dry-run=client -o jsonpath='{.data.\.dockerconfigjson}'
    dockerconfigjson: ""
  
  # Ticketmaster API Secret
  # Only used if secrets.create is true
  ticketmaster:
    # Plain text API key (will be base64 encoded automatically)
    apiKey: "bzdJZmhLR3dJWmpZeEQ5TVFQYkh2WGYxSmNMUG42NDcK"