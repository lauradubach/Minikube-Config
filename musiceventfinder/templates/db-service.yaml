apiVersion: v1
kind: Service
metadata:
  name: msvc-bp-prod-db
  namespace: {{ .Values.namespace | default "musicevents" }}
  labels:
    {{- include "musiceventfinder.labels" . | nindent 4 }}
    app.kubernetes.io/component: db
spec:
  type: ClusterIP
  ports:
  - port: 3306
    targetPort: 3306
    protocol: TCP
    name: mysql
  selector:
    {{- include "musiceventfinder.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: db